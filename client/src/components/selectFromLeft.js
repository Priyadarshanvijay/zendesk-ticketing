import React from 'react'
import {
  Segment
} from 'semantic-ui-react';

const menuBar = () => (
  <Segment basic placeholder textAlign='center'>
    Click on a ticket from left to view it.
  </Segment>
);

export default menuBar;
