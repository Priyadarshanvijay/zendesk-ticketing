import React, { createRef } from 'react'
import { Sticky, Container, Header, Image, Modal, ModalActions, Segment, Menu } from 'semantic-ui-react'

function TicketDetailedView() {

  return (
    <Segment basic fluid>
      <Header>
        This is a post
      </Header>
    </Segment>
  )
}

export default TicketDetailedView;
